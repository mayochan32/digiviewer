# Changelog

DigiViewerの主な変更履歴です。

バージョン番号は次の方針で更新します。

- 機能追加: `x.y.z` の `y` を上げる
- バグ修正: `x.y.z` の `z` を上げる

## 0.6.1 - 2026-09-22

### Changed

- 種名推測・種名付加を類似解析と同じツールバーへ移動し、幅300pxの種名フィールドを追加。種名付加ダイアログの初期値として使い、アプリ終了時にリセットする。
- 推測画面に「種名フィールドに設定」を追加。ファイル名を変更せず確認した名前を再利用でき、ファイル名への追加が成功した場合もフィールドへ反映する。
- マニュアルに候補の読み方と種名フィールドの連携を追記し、既存4画面と推測画面2点を現行UIで撮影。推測画面の数値は説明用の固定データと明記。

### Fixed

- 和名が9種しかなく、多くの候補が学名だけで表示される問題を修正。Wikidata（CC0）由来の12,580学名分を同梱し、和名の出典を確認できるようにした。
- モデルの `Parnara guttatus` をイチモンジセセリ、`Polytremis pellucida` をオオチャバネセセリと表示する、出典付きの学名対応を追加。候補を選ぶと和名が入力される。
- 和名未収録の種は学名を維持し、似た学名から名前を推測しない。辞書は判定結果を表示するときに読み込み、モデルの再導入は不要。
- 和名の回帰テストと実際の表示コンポーネントを操作するブラウザテストを追加。Mac配布時の署名検証も継続。

## 0.6.0 - 2026-09-22（Windows CPU主要動作確認済み）

### macOS配布物の修正（2026-09-22）

- 0.6.0 Mac版ZIPのアプリ全体への仮署名漏れを修正して再梱包。アプリ本体の機能と版番号は変更せず、Windows配布物も変更しない。
- Macの配布用スクリプトでビルド後とZIP再展開後の署名検証を必須化。Developer ID署名・Apple公証はなく、初回起動の許可手順を明記。

### Added

- 設定から導入・有効化・無効化・削除できる、無料の種名推測オプションを追加。
- BioCLIP 2で表示中の写真1枚から上位5候補、相対スコア、写真全体・切り出し・国内記録による補助情報を表示。
- 確認した候補を種名入力欄へ反映し、利用者の操作でファイル名へ追加。
- 専用Python本体・venv・モデルをDigiViewer専用のアプリデータ領域に隔離。既存Python・PATH・レジストリ・GPUドライバーを変更しない導入方式。
- Mac CPU/MPS、Windows x64 CPU/任意のNVIDIA CUDA版の導入経路。Windows 11 x64実機でCPU版の導入・判定・削除を確認済み。NVIDIA版は実機未検証。

### Known limitations

- Windowsで種名判定中にアプリを強制終了すると、専用Pythonプロセスが残る。判定完了後の終了を推奨し、専用プロセスの確認・停止方法を使い方に記載。この制約は修正せず注意事項として許容する。通常のウィンドウ終了での挙動は未検証。
- その他の確認範囲・未検証項目は[Windows検証結果の確認と受け入れ判断](docs/windows-0.6.0-review.md)を参照。

### Maintenance

- 開発・ビルド用のVite、PostCSS、nanoidを互換範囲内で更新し、npm監査の指摘3件を解消。
- 種名推測の操作・終了時の注意事項を日本語マニュアルに追加。

### Independence

- 写真の管理・公開ソフトとは完全に独立。共有サービス、共有設定、相手ソフトの起動・インストールを必要としない。
- 同一の写真ファイルを両方で利用する場合、利用者が行う改名・移動・内容変更は通常のファイル操作として両方から見える。

## マニュアル公開 - 2026-09-21

### Documentation

- v0.5.5を対象とした、画面写真付きの日本語Webマニュアルを追加。大量の写真を撮影・選別するためのコンセプト、基本操作、比較、評価、クロップ、設定、保存データを説明。
- 紹介ページとREADMEにマニュアルへの導線を追加。

## 0.5.5 - 2026-08-03

### Fixed

- サムネイルの横スクロール表示を独自スクロールバーに一本化し、macOSの自動表示設定に左右されない常時表示UIに変更。
- サムネイルが少なくスクロール不要な場合も、独自スクロールバーを無効状態として表示するように調整。

## 0.5.4 - 2026-08-03

### Fixed

- 独自サムネイルスクロールバーを追加した際に、レイアウト行が不足してバーが表示されない問題を修正。
- ネイティブスクロールバーを完全には隠さず、独自バーが表示できない場合でもスクロール位置がわかるように調整。

## 0.5.3 - 2026-08-03

### Fixed

- macOSでサムネイルバーの横スクロールバーが操作中だけ表示される問題を避けるため、常時表示の独自スクロールバーを追加。

## 0.5.2 - 2026-08-02

### Fixed

- サムネイルバーの横スクロールバーが見えるように調整。
- 採用、保留、一時除外などのレビュー状態操作で表示倍率と位置がフィットに戻らないように修正。
- 同じレビュー状態ボタンをもう一度押すと、対象画像の状態を未判定へ戻せるように修正。

## 0.5.1 - 2026-07-14

### Fixed

- 旧形式サムネイルキャッシュの移行時に、キャッシュ未命中のたびフォルダ全体を走査してサムネイル生成が遅くなる問題を修正。

### Changed

- 類似写真レビューの除外状態を「一時除外」と表示し、一時除外の画像をまとめて `deleted` フォルダへ移動できるように変更。

## 0.5.0 - 2026-07-12

### Added

- dHashと色特徴による類似写真の自動グループ化を追加。
- 類似グループ内の複数写真を採用・保留・除外・未判定で管理できるレビュー機能を追加。
- 類似解析キャッシュとレビュー状態を写真フォルダ内の `.digiviewer` に保存し、macOSとWindowsで共有可能にした。

### Changed

- 既存の共有サムネイルを類似解析へ再利用し、大型画像の解析を高速化。
- グループ表示をオフにした際に基本の1枚表示へ戻るよう変更。

### Fixed

- Windows起動直後に一部のサムネイルしか表示されない問題を修正。
- レビュー状態の非同期復元によってサムネイル生成キューが停止する問題を修正。

## 0.4.0 - 2026-06-28

### Added

- 現在開いているフォルダを再読み込みし、追加された画像だけを一覧に加えるリロード機能を追加。
- リロードで追加された画像のサムネイルキャッシュを自動作成するように追加。

## 0.3.0 - 2026-06-28

### Added

- crop範囲を選択後、範囲内をドラッグしてサイズを保ったまま移動できるように追加。

## 0.2.1 - 2026-06-22

### Fixed

- crop保存時に元画像のEXIF、XMP、ICCプロファイルを引き継ぐように修正。
- crop保存後のファイル更新日時を元画像に合わせるように修正。

## 0.2.0 - 2026-06-05

### Added

- macOS版DigiViewerの基本機能を整理。
- 複数枚比較表示、同期ズーム/移動、サムネイル表示、EXIF表示を追加。
- サムネイルキャッシュ作成、整理、削除機能を追加。
- ファイル名フィルタ、選択画像への種名付加、Finderへ貼り付け可能なファイルコピーを追加。
- チェックした画像を `deleted` フォルダへ移動して一覧から除外する機能を追加。
- crop保存機能を追加。
  - `任意`、`1:1`、`3:2`、`4:3` の縦横比指定。
  - 2倍upscale指定。
  - 元画像と同じフォルダへ `_crop.jpg` として保存。
  - crop画像をサムネイル一覧に追加。

### Changed

- サムネイルのフォーカス枠を赤、チェック枠を緑に変更。
- 複数枚表示から1枚表示へ戻した時、表示画像とサムネイルフォーカスがずれないように調整。
- crop画像を元画像の右隣に並ぶようにサムネイルのソート順を調整。
- フォルダ内サムネイルキャッシュをmacOS/Windows間で共有しやすい形式に変更。

### Fixed

- Windows版ビルド成果物がlocalhostへ接続しようとする問題を修正。
- Windows版でのファイルコピー、キーボードショートカット、サムネイルキャッシュ共有に関する問題を修正。
- ファイル名変更時にサムネイルキャッシュを作り直さず引き継ぐように修正。
